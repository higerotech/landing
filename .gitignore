# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Logs y temporales
*.log
tmp/
.cache/

# Dependencias de las pruebas. El sitio no las usa: no se construye con npm.
# `package-lock.json` SI se versiona — hace reproducible el CI y es lo que
# escanea el gate de dependencias.
node_modules/

# Artefactos de build / escaneo
dist/
coverage/
test-results/
playwright-report/
blob-report/
.playwright/
sbom.json
trivy-report.*
*.tfstate*
.terraform/

# Secretos — nunca versionar
.env
.env.*
!.env.example
